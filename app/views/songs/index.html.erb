<div class="new-form">
  <% form_for(@song) do |f| %>
    <%= f.error_messages %>
    <%= f.text_field :name %>
    <p>
      <%= f.submit "Create" %>
    </p>
  <% end %>
</div>

<p style="color: green"><%= flash[:notice] %></p>

<table>
<% for song in @songs %>
  <tr>
    <td><%= song.name %></td>
  </tr>
<% end %>
</table>

<br />
